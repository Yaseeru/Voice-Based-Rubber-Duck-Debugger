# ============================================
# Voice-Based Rubber Duck Debugger
# Environment Configuration
# ============================================

# --------------------------------------------
# REQUIRED: Google Cloud Configuration
# --------------------------------------------
# Your Google Cloud Project ID where Vertex AI is enabled
# Find this in: https://console.cloud.google.com/
# Required for: Gemini AI model access via Vertex AI
GOOGLE_CLOUD_PROJECT=your_gcp_project_id

# The region where your Vertex AI resources are located
# Common values: us-central1, us-east1, europe-west1, asia-southeast1
# Default: us-central1
VERTEX_AI_LOCATION=us-central1

# --------------------------------------------
# OPTIONAL: ElevenLabs API Configuration
# --------------------------------------------
# Get your API key from: https://elevenlabs.io/app/settings/api-keys
# This key is used for Text-to-Speech (TTS) service
# If not provided, the app will work without audio responses (text-only)
# ELEVENLABS_API_KEY=your_elevenlabs_api_key_here

# --------------------------------------------
# OPTIONAL: Redis Configuration
# --------------------------------------------
# Redis connection URL for session storage (optional)
# If not provided, the system will use in-memory session storage
# Format: redis://[username:password@]host:port[/database]
# Example: redis://localhost:6379
# Example with auth: redis://user:password@redis-host:6379/0
# REDIS_URL=redis://localhost:6379

# --------------------------------------------
# Server Configuration
# --------------------------------------------
# Application environment (development, production, test)
NODE_ENV=development

# Port for the backend server to listen on
# Default: 8080 (required for Google Cloud Run)
PORT=8080

# Session timeout in milliseconds (1 hour = 3600000ms)
# Sessions inactive for this duration will be cleaned up
SESSION_TIMEOUT=3600000

# --------------------------------------------
# CORS Configuration
# --------------------------------------------
# Frontend URL for CORS configuration
# This should match where your frontend is hosted
# Development: http://localhost:3000
# Production: https://your-frontend-domain.com
FRONTEND_URL=http://localhost:3000

# --------------------------------------------
# Frontend Configuration
# --------------------------------------------
# Backend API URL for frontend to connect to
# This is used by Vite to configure the API endpoint
# Development: http://localhost:8080
# Production: https://your-backend-domain.com
VITE_API_URL=http://localhost:8080
